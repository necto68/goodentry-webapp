export enum RoutePathname {
  PROTECTED_PERPS = "/",
  EZ_VAULTS = "/ezvaults",
  EZ_VAULT_DETAILS = "/ezvault/:vaultId",
  DASHBOARD = "/dashboard",
  PUBLIC_SALE = "/fair-sale",
  LOCK = "/lock",
}
