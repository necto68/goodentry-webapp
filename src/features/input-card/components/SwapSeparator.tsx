import { swapArrow } from "../../icons/input-card";
import { Container, ArrowIcon } from "../styles/SwapSeparator";

export const SwapSeparator = () => (
  <Container>
    <ArrowIcon src={swapArrow} />
  </Container>
);
