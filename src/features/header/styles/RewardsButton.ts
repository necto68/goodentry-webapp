import { Button } from "@chakra-ui/react";
import styled from "@emotion/styled";
import tw from "twin.macro";

export const RewardsButtonContainer = styled(Button)`
  ${tw`flex gap-2 items-center justify-center`}
`;
