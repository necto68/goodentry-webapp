import type { TokenInputState } from "../../input-card/types/TokenInputState";

export interface PublicSaleModalState {
  collateralTokenInputState: TokenInputState;
  saleTokenInputState: TokenInputState;
}
